# Base database URL (production / default)
# DATABASE_URL=postgresql://user:pass@host:5432/prod_db

# Development override connection string (Neon example shown)
# When DB_MODE=dev (or USE_DEV_DB=1/true) this value is used instead of DATABASE_URL
DEV_DATABASE_URL=postgresql://neondb_owner:npg_nYbqRxpE4B3j@ep-snowy-term-a1b41zre-pooler.ap-southeast-1.aws.neon.tech/gogaji?sslmode=require&channel_binding=require

# Flag to switch
# Option A:
# DB_MODE=dev
# Option B:
# USE_DEV_DB=1

# Disable SSL enforcement (not recommended on cloud) by uncommenting:
# DB_SSL_DISABLE=false

# CORS origin
CORS_ORIGIN=http://localhost:5173

# JWT secret etc.
# JWT_SECRET=change_me
